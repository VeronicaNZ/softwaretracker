<h2>User</h2>
<table>
  <tr>
    <th>First name</th>
    <th>Surname</th>
    <th>Action</th>
    <th></th>
    <th></th>
  </tr>
  <tr>
    <td><%= @user.first_name %></td>
    <td><%= @user.sur_name %></td>
    <td><%= link_to "Show", @user %></td>
    <td><%= link_to "Edit", edit_user_path(@user) %></td>
   <!-- <td><%= link_to "Remove", @user, method: :destroy, data: { confirm: "Are you sure?" } %></td> -->
    <td><%= link_to "Destroy", @user, data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %></td>
  </tr>
</table>